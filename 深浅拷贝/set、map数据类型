es6新出的set和map两种结构，保证每组成员的唯一性

set和map的区别在于，set只有键值，没有键名，所以导致了在遍历器遍历的时候，键名和键值一致的情况，在添加成员时只有add（）函数进行添加，而map有键值和键名，
添加成员时，只有set（）函数进行添加，get（）方法进行取值。在set中，仅仅接受数组和具有遍历器的对象，map接受各种类型数据结构

- map：任何数据类型

- set：数组及具有遍历器的对象

- weakMap、weakSet：仅引用类型

weakMap和weakSet不会进入垃圾回收，会自动被系统清理
